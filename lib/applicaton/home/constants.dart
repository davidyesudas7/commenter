import 'package:flutter/material.dart';

const kTopBoxDecration = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.only(
        bottomLeft: Radius.circular(40), bottomRight: Radius.circular(40)));

const kTitleBoxDecration = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.only(
        bottomLeft: Radius.circular(40), bottomRight: Radius.circular(40)));

const kTitleTextStyle =
    TextStyle(color: Colors.black, fontSize: 25, fontWeight: FontWeight.w500);

const kSpaceBox = SizedBox(
  height: 20,
);

const kCommentSpaceBox = SizedBox(
  width: 10,
);
const kCommentBoxPadding = EdgeInsets.only(top: 20, left: 15, right: 15);
const kBottomBoxColur = Color(0xffEDEDED);
